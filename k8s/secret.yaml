apiVersion: v1
kind: Secret
metadata:
  name: dunkin-secrets
  namespace: dunkin-voice
type: Opaque
stringData:
  # Azure OpenAI credentials (still needed for cloud LLM)
  AZURE_OPENAI_EASTUS2_ENDPOINT: "wss://<your-endpoint>.openai.azure.com"
  AZURE_OPENAI_EASTUS2_API_KEY: "<your-api-key>"
