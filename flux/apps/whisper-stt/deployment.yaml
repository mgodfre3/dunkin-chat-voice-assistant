apiVersion: apps/v1
kind: Deployment
metadata:
  name: whisper-stt
  namespace: dunkin-voice
  labels:
    app.kubernetes.io/name: whisper-stt
    app.kubernetes.io/component: speech-to-text
spec:
  replicas: 1
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app.kubernetes.io/name: whisper-stt
  template:
    metadata:
      labels:
        app.kubernetes.io/name: whisper-stt
        app.kubernetes.io/component: speech-to-text
    spec:
      # Run on dedicated GPU node for CUDA acceleration
      nodeSelector:
        accelerator: nvidia
      # Prefer the node NOT running Phi-4 (spread across GPUs)
      affinity:
        podAntiAffinity:
          preferredDuringSchedulingIgnoredDuringExecution:
            - weight: 100
              podAffinityTerm:
                labelSelector:
                  matchLabels:
                    app.kubernetes.io/name: phi-4-mini-gpu
                topologyKey: kubernetes.io/hostname
      containers:
        - name: whisper
          image: fedirz/faster-whisper-server:latest-cuda
          ports:
            - containerPort: 8000
              name: http
          env:
            - name: WHISPER__MODEL
              value: "Systran/faster-whisper-large-v3-turbo"
            - name: WHISPER__INFERENCE_DEVICE
              value: "cuda"
            - name: WHISPER__COMPUTE_TYPE
              value: "float16"
            - name: PRELOAD_MODELS
              value: '["Systran/faster-whisper-large-v3-turbo"]'
            - name: WHISPER__TTL
              value: "-1"
            - name: DEFAULT_LANGUAGE
              value: "en"
            - name: HF_HUB_OFFLINE
              value: "0"
          resources:
            requests:
              cpu: "1"
              memory: "2Gi"
              nvidia.com/gpu: 1
            limits:
              cpu: "3"
              memory: "4Gi"
              nvidia.com/gpu: 1
          volumeMounts:
            - name: model-cache
              mountPath: /root/.cache/huggingface
          livenessProbe:
            httpGet:
              path: /health
              port: 8000
            initialDelaySeconds: 120
            periodSeconds: 30
            timeoutSeconds: 10
          readinessProbe:
            httpGet:
              path: /health
              port: 8000
            initialDelaySeconds: 60
            periodSeconds: 15
            timeoutSeconds: 10
      volumes:
        - name: model-cache
          emptyDir:
            sizeLimit: 10Gi
